include_directories(server)
add_subdirectory(server)
add_executable(mmc main.cpp)
target_link_libraries(mmc mmc_server)
if (NOT WIN32)
	target_link_libraries(mmc boost_program_options-mt)
endif (NOT WIN32)
